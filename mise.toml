[tools]
bun = "1.3.9"

[tasks.build]
description = "Bundle revoid into dist/revoid.js (requires Bun at runtime)"
run = "bun run build"

[tasks.compile]
description = "Compile revoid into a standalone binary (~100MB, embeds the Bun runtime)"
run = "bun run build:binary"
